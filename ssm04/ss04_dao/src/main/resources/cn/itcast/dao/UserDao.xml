<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.itcast.dao.UserDao">
    <select id="findByUid" parameterType="int" resultType="user">
        select * from tab_user where uid = #{a}
    </select>

    <select id="saveUserd" parameterType="user">
        insert into tab_user(nam,sex,telephone,birthday) VALUES (#{name},#{sex},#{telephone},#{birthday})
    </select>
    <select id="findAll" resultType="user">
        select * from user;
    </select>
</mapper>